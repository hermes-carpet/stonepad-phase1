name: Client Pre-release

on:
  workflow_run:
    workflows: ["Client CI"]
    types: [completed]
    branches: [main]

jobs:
  pre-release:
    name: Publish Pre-release APK
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read
    steps:
      - name: Checkout (for tag computation only)
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Read version
        id: version
        run: |
          VER=$(grep '^version:' client/pubspec.yaml | awk '{print $2}')
          echo "ver=$VER" >> $GITHUB_OUTPUT
          echo "pubspec version: $VER"

      - name: Check if pre-release version
        id: check
        run: |
          VER="${{ steps.version.outputs.ver }}"
          PATCH=$(echo "$VER" | cut -d. -f3)
          PATCH="${PATCH:-0}"
          if [ "$PATCH" != "0" ]; then
            echo "pre_release=true" >> $GITHUB_OUTPUT
            echo "Pre-release version ($VER, patch=$PATCH) — will publish"
          else
            echo "pre_release=false" >> $GITHUB_OUTPUT
            echo "Stable version ($VER, patch=0) — skipping pre-release"
          fi

      - name: Compute tag
        if: steps.check.outputs.pre_release == 'true'
        id: tag
        run: |
          TAG="v${{ steps.version.outputs.ver }}"
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "tag=$TAG"

      - name: Download APK from CI
        if: steps.check.outputs.pre_release == 'true'
        uses: dawidd6/action-download-artifact@v4
        with:
          workflow: client-ci.yml
          run_id: ${{ github.event.workflow_run.id }}
          name: stonepad-apk
          path: /tmp/apk

      - name: Verify APK
        if: steps.check.outputs.pre_release == 'true'
        run: |
          APK=$(ls /tmp/apk/stonepad-*.apk 2>/dev/null | head -1)
          echo "=== APK verification ==="
          if [ -z "$APK" ] || [ ! -f "$APK" ]; then
            echo "ERROR: APK not found in /tmp/apk/"
            ls -la /tmp/apk/
            exit 1
          fi
          echo "APK: $APK"
          SIZE=$(stat -c%s "$APK")
          echo "Size: $SIZE bytes ($(echo "scale=1; $SIZE/1048576" | bc) MB)"
          if [ "$SIZE" -lt 10000000 ]; then
            echo "ERROR: APK too small (< 10 MB), likely corrupted"
            exit 1
          fi
          # Verify it's a valid ZIP (APK is ZIP)
          if ! unzip -t "$APK" > /dev/null 2>&1; then
            echo "ERROR: APK is not a valid ZIP archive"
            exit 1
          fi
          echo "APK is valid ZIP"
          echo "=== Verification passed ==="
          echo "apk_path=$APK" >> $GITHUB_ENV

      - name: Publish pre-release
        if: steps.check.outputs.pre_release == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: "Stonepad ${{ steps.tag.outputs.tag }}"
          body: |
            ⚠️ **Pre-release** — auto-published from CI on `main`.

            | | |
            |---|---
            | Version | `${{ steps.version.outputs.ver }}`
            | Commit | `${{ github.event.workflow_run.head_sha }}`

            **Install**: Download the APK and sideload on Android.
          prerelease: true
          make_latest: false
          files: ${{ env.apk_path }}
